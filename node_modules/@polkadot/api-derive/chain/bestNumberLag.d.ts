import type { Observable } from 'rxjs';
import type { BlockNumber } from '@polkadot/types/interfaces/index.js';
import type { DeriveApi } from '@polkadot/api-derive/types.js';
/**
 * @name bestNumberLag
 * @returns A number of blocks
 * @description Calculates the lag between finalized head and best head
 * @example
 * <BR>
 *
 * ```javascript
 * api.derive.chain.bestNumberLag((lag) => {
 *   console.log(`finalized is ${lag} blocks behind head`);
 * });
 * ```
 */
export declare function bestNumberLag(instanceId: string, api: DeriveApi): () => Observable<BlockNumber>;
